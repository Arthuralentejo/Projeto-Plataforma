const dbConnection = require('../DAL/dbConnection')

class Students {
    constructor(name, age, addres, ) {
        this.name = name
        this.age = age
    }

    save(students) {


    }
    update() {

    }
}